{
  "name": "tangleflow",
  "version": "0.0.0-dev",
  "description": "Converts GitHub actions into Tangled workflows and vice versa.",
  "main": "./lib/main.js",
  "bin": {
    "tangleflow": "./lib/cli.js"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./lib/main.d.ts",
      "default": "./lib/main.js"
    }
  },
  "files": [
    "lib",
    "!lib/**/*.test.js",
    "!lib/**/*.test.d.ts"
  ],
  "scripts": {
    "build:clean": "node -e \"require('fs').rmSync('./lib', { recursive: true, force: true })\"",
    "build": "npm run build:clean && tsc",
    "lint:js": "oxlint src scripts",
    "lint:format": "oxfmt --check src scripts",
    "lint": "npm run lint:js && npm run lint:format",
    "format": "oxfmt --write src scripts",
    "test": "vitest run",
    "update-github-types": "node scripts/update-github-types.ts && npm run format",
    "update-action-refs": "node scripts/update-action-refs.ts && npm run format"
  },
  "keywords": [
    "tangled",
    "actions",
    "gha",
    "github"
  ],
  "author": "James Garbutt (https://github.com/43081j)",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/43081j/tangleflow#readme",
  "bugs": {
    "url": "https://github.com/43081j/tangleflow/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/43081j/tangleflow.git"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.5",
    "json-schema-to-typescript": "^15.0.4",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
